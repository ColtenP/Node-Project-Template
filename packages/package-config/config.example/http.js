//
// Package HTTP Configuration
//

module.exports = {
    // Hostname
    // Address the HTTP server will listen for requests on
    // Default: 0.0.0.0 - Listens on all addresses
    hostname: '0.0.0.0',

    // Port
    // Port the HTTP server will listen for requests on
    // Default: 8123
    port: 8123
};
